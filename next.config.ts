import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Tidak perlu konfigurasi khusus untuk service worker
  // Next.js secara otomatis melayani file dari public directory
};

export default nextConfig;
